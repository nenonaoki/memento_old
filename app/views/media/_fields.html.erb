<%= render "shared/error_messages", m: @medium %>

<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :source %>
<%= f.text_field :source %>

<%= f.label :description %>
<%= f.text_area :description %>

<%= label_tag :tags %>
<%#= f.collection_select :tags, Tag.all, :id, :label, {}, multiple: true %>
<%#= f.select :tags, Tag.all.map{ |tag| [tag.label, tag.id] }, {},  multiple: true %>
<%= select_tag :tags, options_for_select(Tag.all.map{ |tag| [tag.label, tag.id] }),  multiple: true %>
