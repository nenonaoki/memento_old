<% if m.errors.any? %>
  <div class="alert alert-danger">
    The form contains <%= pluralize(m.errors.count, "error") %>.
  </div>
  <ul>
  <% m.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
<% end %>